# auto-note

## 作業の入口

- 記事は `note-article-seo-draft`、ネタ補充は `note-topic-ideas`、投稿は `note-article-publish` を使う。
- 記事作成・投稿時は [編集方針](docs/editorial-policy.md) と [本文形式](docs/note-format.md) を読む。
- 初期設定・障害対応は [README.md](README.md)。正常な記事作成で読み直さない。
- `.agents/skills/` と `.codex/agents/` は用途が異なる。統合しない。

## 品質の必須条件

- 本人の一次体験・実績を記事に書かない。根拠のないデータ・経験を作らない。
- 本文は4,000〜6,000字。文体・SEO・計数の詳細は編集方針と本文形式を適用する。
- 調査→設計→執筆→独立監査の順序を記事ごとに守る。同じ工程なら異なる記事の専任agentを同時に呼んでよい。1記事内では前工程の結果が確定する前に次工程を始めない。
- manifest登録とstate更新は親だけが行う。専任agentへ委譲せず、同時書き込みを起こさない。
- 監査指摘と未確定タグを解消してから投稿する。速度のために検証・事実確認を省略しない。
- 修正→検証（check_article.mjs）と修正→監査（Phase 7）の往復は各3回まで、本文画像の再生成は各2回までとする。上限到達時は打ち切り、未解決点を明記して報告に進む。
- 新規・リライト記事には、内容理解を助ける本文画像を2〜3枚入れる。article-visual-generatorへ委譲し、本文との整合・文字・可読性・配置を目視検品する。
- 複数枚の記事を作成する依頼では、記事ごとに異なる画像を新規生成する。同じ画像を複数記事で使い回さない。
- アイキャッチはeyecatch-generatorへ委譲。参考HTMLを基にimage_genで新規生成し、目視検品した1280×670pxのPNGを必ず添付する。
- 参考HTMLのコピー・文言置換・レンダリングによる画像作成は禁止。

## 公開と権限

- 通常の依頼は監査・検証後に本人のnoteへ下書き保存して停止する。「本公開」が明示された場合だけ本公開する。
- 「ローカルのみ」「投稿しない」と無人実行はnoteへ送らず、ローカルで監査・検証・state更新後にmanifestを確定して停止する。作業ファイルの削除はnote保存・公開が検証成功した場合に限る。
- MCPはnote_session_status、open_note_login、publish_noteの3ツールのみ。
- ログインは本人が手動で行う。セッションCookieを抽出せず、CSRF用XSRF-TOKENだけを使う。ログイン待ち（open_note_login→note_session_status）は無期限に続けない。最大3回の再確認で未ログインならタイムアウトとして停止し、状況を報告する。
- 削除・公開取り消し等のMCP接続、いいね・フォロー・コメント操作を追加しない。
- 公開状態と画像URLを検証する。結果不明・部分成功では再投稿せず記録を報告する。

## 開発

- 記事・画像・stateの既存変更を保持する。テスト投稿は別途依頼がある場合のみ。
- 記事作業の開始時にartifact manifestを作り、今回`articles/`へ新規追加したMarkdown・画像だけを登録する。note保存・公開が検証成功した記事だけ、報告内容を確定した後で登録済みファイルを削除する。ローカルのみで終える場合はmanifestを確定するだけで作業ファイルを消さない。既存・未登録・復旧用ファイルも消さない。
- 手順変更では品質要件の移設先を照合する。コード変更はnpm testで回帰確認する。
